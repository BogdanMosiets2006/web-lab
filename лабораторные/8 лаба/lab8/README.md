# Фото Галерея - Лабораторная работа №8

Веб-приложение для управления галереей изображений с использованием PHP, MySQL и паттерна MVC.

## Функциональность

- Просмотр галереи фотографий (миниатюры)
- Просмотр фотографий в полном размере
- Загрузка новых фотографий
- Счетчик просмотров для каждой фотографии
- Сортировка по популярности (количеству просмотров)
- Хранение информации о пользователях
- Автоматическое создание миниатюр
- Адаптивный дизайн

## Структура проекта

```
gallery/
├── config/                  # Конфигурация
│   ├── config.php          # Настройки приложения
│   └── Database.php        # Класс для работы с БД
├── controllers/            # Контроллеры (MVC)
│   └── GalleryController.php
├── models/                 # Модели (MVC)
│   ├── User.php
│   └── Photo.php
├── views/                  # Представления (MVC)
│   ├── index.php
│   ├── photo.php
│   └── upload.php
├── public/                 # Публичная директория
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   ├── uploads/
│   │   ├── original/       # Оригиналы фото
│   │   └── thumbnails/     # Миниатюры
│   ├── index.php           # Точка входа - галерея
│   ├── photo.php           # Просмотр фото
│   └── upload.php          # Загрузка фото
└── database/
    └── schema.sql          # SQL схема БД
```

## 🚀 Установка и запуск

### 1. Требования

- PHP 7.4 или выше
- MySQL 5.7 или выше
- Apache
- Расширения PHP: PDO, GD

### 2. Установка базы данных

1. Создайте базу данных MySQL:

2. Импортируйте схему:
```sql
source database/schema.sql;
```

Или используйте phpMyAdmin для импорта файла `database/schema.sql`

### 3. Настройка конфигурации

Отредактируйте файл `config/config.php` и укажите параметры подключения к БД:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'photo_gallery');
define('DB_USER', 'root');
define('DB_PASS', 's8aC6r7_Fe');
define('DB_CHARSET', 'utf8mb4');
```

### 4. Настройка прав доступа

Убедитесь, что директории для загрузки имеют права на запись:

```bash
chmod -R 755 public/uploads/
```

### 5. Настройка веб-сервера

#### Apache (.htaccess уже включен)

Убедитесь, что mod_rewrite включен:
```bash
sudo a2enmod rewrite
sudo service apache2 restart
```

### 6. Запуск

Откройте в браузере:
```
http://localhost/lab8/gallery/public/
```

## Использование

### Просмотр галереи
- Откройте главную страницу `index.php`
- Фотографии отсортированы по популярности (количеству просмотров)
- Нажмите на фото для просмотра в полном размере

### Загрузка фотографии
1. Нажмите кнопку "Загрузить фото"
2. Заполните форму:
   - Ваше имя
   - Email
   - Название фотографии
   - Описание (необязательно)
   - Выберите файл (JPG, PNG, GIF, до 5MB)
3. Нажмите "Загрузить фото"

### Просмотр фотографии
- Нажмите на миниатюру в галерее
- Отображается:
  - Фотография в полном размере
  - Название и описание
  - Количество просмотров
  - Информация об авторе
  - Дата загрузки

## Архитектура (MVC)

### Model (Модель)
- `User.php` - работа с пользователями
- `Photo.php` - работа с фотографиями

### View (Представление)
- `index.php` - галерея (список фото)
- `photo.php` - просмотр фото
- `upload.php` - форма загрузки

### Controller (Контроллер)
- `GalleryController.php` - основная логика приложения

## Структура базы данных

### Таблица `users`
```sql
- id (PRIMARY KEY)
- username (VARCHAR)
- email (VARCHAR)
- created_at (TIMESTAMP)
```

### Таблица `photos`
```sql
- id (PRIMARY KEY)
- user_id (FOREIGN KEY → users.id)
- title (VARCHAR)
- description (TEXT)
- filename (VARCHAR)
- thumbnail (VARCHAR)
- views (INT)
- created_at (TIMESTAMP)
```

## Особенности реализации

### ООП
- Использование классов и объектов
- Паттерн Singleton для подключения к БД
- Инкапсуляция логики в модели

### Безопасность
- Prepared statements (защита от SQL-инъекций)
- Валидация данных на сервере
- XSS-защита через htmlspecialchars()
- Проверка типов и размеров файлов

### Производительность
- Автоматическое создание миниатюр
- Индексы в БД для быстрой сортировки
- Ленивая загрузка изображений (JavaScript)

## Дизайн

- Современный градиентный дизайн
- Адаптивная верстка (responsive)
- Анимации и плавные переходы
- Красивые hover-эффекты

## Выполненные требования

**Основное задание (+8 баллов):**
- Создана страница index.php с просмотром всех фото
- Создана страница photo.php с просмотром конкретной фото
- Создана страница upload.php с формой загрузки
- Таблица photos для хранения информации о картинках
- Таблица users для хранения информации о пользователях
- Счетчик просмотров под каждой фотографией
- Сортировка по популярности (количеству кликов)

## Возможные улучшения

- Пагинация для больших галерей
- Система комментариев
- Категории и теги
- Поиск по фотографиям
- Личные кабинеты пользователей
- Редактирование и удаление фото
- AJAX-загрузка без перезагрузки страницы

## ‍Автор

Студент 3 курса, группы ИВТ-1, Мосиец Богдан 
Вариант 11 

## Дата создания

Январь 2025
